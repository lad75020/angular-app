<!doctype html>
<html lang="en" ng-app="app">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Xanax</title>
    <link rel="stylesheet" href="/js/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body ng-controller="AuthController as vm">
    <div class="page-bg"></div>
    <div class="container py-5 position-relative">
      <header class="mb-4 text-center">
        <h1 class="display-6 fw-semibold">Xanax</h1>
        <p class="text-muted">Daily or weekly counts for log 2290 and 2291.</p>
      </header>

      <div class="status-card mb-4">
        <div class="d-flex flex-column flex-lg-row align-items-lg-center justify-content-between">
          <div>
            <p class="mb-1 fw-semibold">Session</p>
            <p class="mb-0 text-muted" ng-if="!vm.user">Checking session…</p>
            <p class="mb-0 text-muted" ng-if="vm.user">
              Logged in as <strong>{{ vm.user.email }}</strong>
            </p>
          </div>
          <%- include('partials/nav') %>
        </div>
      </div>

      <div class="card shadow-sm" ng-if="vm.user">
        <div class="card-body">
          <div class="d-flex flex-column flex-lg-row align-items-lg-center justify-content-between mb-3">
            <div>
              <h2 class="h5 mb-1">Xanax activity</h2>
            </div>
            <div class="revives-controls mt-3 mt-lg-0">
              <select
                class="form-select"
                ng-model="vm.xanax.granularity"
                ng-change="vm.loadXanax(vm.xanax.granularity)"
              >
                <option value="daily">Daily</option>
                <option value="weekly">Weekly</option>
              </select>
              <button class="btn btn-outline-secondary" ng-click="vm.loadXanax(vm.xanax.granularity)">
                Refresh
              </button>
              <button
                class="btn btn-outline-primary"
                ng-click="vm.toggleXanaxLast30()"
              >
                30 days
              </button>
            </div>
          </div>

          <div class="revives-status text-muted" ng-if="vm.xanax.loading">
            Loading xanax data...
          </div>
          <div class="revives-status text-muted" ng-if="!vm.xanax.loading && vm.xanax.empty">
            No xanax logs found. Click “Logs” to refresh IndexedDB.
          </div>
          <div class="revives-status" ng-if="vm.xanax.last30Only">
            <label class="form-label mb-1">Total xanax (last 30 days)</label>
            <input class="form-control" type="text" ng-value="vm.xanax.last30Total" readonly />
          </div>
          <div id="xanaxChart" class="chart-canvas"></div>
        </div>
      </div>
    </div>

    <%- include('partials/toast') %>
    <%- include('partials/logs-modal') %>

    <script src="/js/angular.min.js"></script>
    <script src="/js/d3.v7.min.js"></script>
    <script src="/js/idb-8.0.0.umd.js"></script>
    <script src="/js/logs-db.js"></script>
    <script src="/js/app.js"></script>
  </body>
</html>
